# Clean Isaac Lab Local Insert

这个子项目是一个干净重建版，只保留教程里最重要的任务定义：

- 机械臂
- 固定在末端的 peg
- 固定在地面的带孔物体
- 外部深度相机
- RL 只学局部对齐和插入

当前目标是先把任务结构和项目结构搭干净，再逐步验证可视化、reset、reward 和训练稳定性。

当前版本采用教程式 3D 小位移动作：策略只控制末端平移，姿态固定保持。
同时，带孔物体底面贴地放置，机械臂默认姿态调整为 peg 朝下、靠近孔口的局部插入起始姿态。
